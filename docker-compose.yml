services:
  db:
    image: "postgres:15"
    restart: always
    environment:
      POSTGRES_USER: "${DB_USER}"
      POSTGRES_PASSWORD: "${DB_PASSWORD}"
      POSTGRES_DB: "${DB_NAME}"
    volumes:
      - "postgres_data:/var/lib/postgresql/data"
    # La porta 5432 è rimossa per sicurezza, il backend la vede comunque

  backend:
    build: .
    restart: always
    depends_on:
      - db
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 5
    # Le variabili d'ambiente vengono iniettate automaticamente da Coolify
    command: sh -c "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port 8000 --proxy-headers --forwarded-allow-ips=* --root-path /"
